{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the ferret shell window. The UI only talks to the ferret server (CORS is open there), so no http plugin scope is needed.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
